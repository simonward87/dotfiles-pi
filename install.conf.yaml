- defaults:
    link:
      relink: true
      create: true
    shell:
      stdout: true
      stderr: true

- clean: ['~']

- link:
    ~/.config/tmux: config/tmux
    ~/.bashrc:
      force: true
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.vimrc:

- shell:
    - command: ./scripts/setup_env.zsh
    - command: ./scripts/setup_docker.zsh
